@AGENTS.md

# Cho Claude Code

- Luật ở `AGENTS.md`; cách làm việc ở `docs/README.md`. Sửa tài liệu xong thì chạy `npm run docs:lint`, sửa mã xong thì `npm run check`, trước khi nói "xong".
- Quyết định mới: thêm mục vào `LOG.md` đúng phạm vi theo `docs/shared/templates/LOG-entry.md`, `Trạng thái: đề xuất`; không tự đổi trạng thái.
